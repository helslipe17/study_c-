#include <iostream>
#include <fstream>
#include <string>
using namespace std;
const int SIZE = 35;


struct ozvl{
//    char name[SIZE];
    string name;
    int donations;
};

int main(){

    ifstream inFile;
    inFile.open("carinfo.txt");

    if(!inFile.is_open()){
        cout << "Не получилось открыть файл!" << endl;
        exit(EXIT_FAILURE);
    }

    int countDonations;
    int countGrangPatrons = 0;
    int countPatons = 0;
    cout << "Введите количество меценатов: ";
    inFile >> countDonations;
    cout << countDonations << endl;
    ozvl *ps = new ozvl[countDonations];
    for (int i = 0; i < countDonations && inFile.good(); i++){
        cout << "Введите имя мецената №" << i + 1 << ": " ;
        inFile >> ps[i].name;
//        inFile.get(ps[i].name);
        cout << ps[i].name;
        cout << "\nВведите количество пожертвований: ";
        inFile >> ps[i].donations;
        cout << ps[i].donations << endl;
    }
    cout << "Gгand Patгons" << endl;
    for (int i = 0; i < countDonations; i++){
        if(ps->donations >=10000){
            cout << ps[i].name << endl;
            countGrangPatrons++;
        }ps--;
    }
    if(countGrangPatrons == 0)
        cout << "none" << endl;

    cout << "Patгons" << endl;
    for (int i = 0; i < countDonations; i++){
        if(ps[i].donations < 10000){
            cout << ps[i].name << endl;
            countPatons ++;
        }
    }
    if(countPatons == 0)
        cout << "none" << endl;

    
    cout << endl;



    return 0;
}
