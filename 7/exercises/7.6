#include <iostream>

using namespace std;

const int MAX = 10;

int fill_array(double arr[], int limit);
void show_array(const double arr[], const int i);
void reverse_array(const double arr[], const int limit);

int main(){
    double arrayEx[MAX];
    int countArray = fill_array(arrayEx, MAX);
    show_array(arrayEx, countArray);
    reverse_array(arrayEx, countArray);
    cout << endl;
    return 0;
}

int fill_array(double arr[], int limit){
    int i;
    int temp;
    for(i = 0; i < limit; i++){
        cout << "Введите double значение №" << i + 1 <<": ";
        cin >> temp;
                if (!cin){
                    cin.clear();
                    while (cin.get()!= '\n')
                        continue;
                    cout << "Неправильный ввод; процесс ввода прекращен.\n";
                    break;
                }
                arr[i] = temp;
    }
    return i;
}

void show_array(const double arr[], const int limit){
    cout << "Содержимое массива: ";
    for(int i = 0; i < limit; i++){
        cout << arr[i] << " ";
    }
    cout << endl;
}
void reverse_array(const double arr[], const int limit){
    cout << "Содержимое массива в обратном порядке: ";
    for(int i = (limit-1); i >= 0; i--){
        cout << arr[i] << " ";
    }
    cout << endl;
}



